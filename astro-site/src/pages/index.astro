---
import Layout from "../layouts/Layout.astro";
import MediaPlaceholder from "../components/MediaPlaceholder.astro";
import { getPageSections } from "../lib/pageSections";
import { safeFetch } from "../lib/safeFetch";
import { inlineHtmlFromBlock } from "../lib/portableText";
import { urlForImage } from "../lib/sanityImage";

const sections = await getPageSections("hjem");

const bookingUrl =
  "https://psno-patient-platform-fe.svc.pasientsky.no/embedded/planner/booking?serviceProviderId=54907264-049e-11eb-8fc8-26c6f94d64b7";

// "Siste artikler" — henter dynamisk fra Sanity (blogPost), viser de 3
// nyeste. Bruker den vanlige offentlige klienten (ikke blogPreviewClient) —
// forsiden skal aldri vise upubliserte utkast.
const latestPosts = await safeFetch(
  `*[_type == "blogPost"] | order(publishedDate desc)[0...3]{
    title, "slug": slug.current, excerpt, category, publishedDate, coverImage
  }`,
  {},
  [],
);

function formatDate(dateString) {
  if (!dateString) return "";
  return new Date(dateString).toLocaleDateString("nb-NO", { year: "numeric", month: "long", day: "numeric" });
}

// Kort 2 og 4 peker til artikler som ikke finnes ennå (forstå-diagnosen og
// restsvimmelhet-etter-krystallsyke) — avklart med Marie: bygg kortene med
// "#" midlertidig, tydelig merket, i stedet for å gjette en URL eller peke
// til en unøyaktig erstatningsside.
const journeyCards = [
  {
    title: "Jeg tror jeg har krystallsyke",
    desc: "Kjenn igjen de typiske tegnene og finn ut om det du opplever passer med krystallsyke.",
    href: "/blogg/er-dette-krystallsyke/",
    comingSoon: false,
  },
  {
    title: "Jeg har fått diagnosen",
    desc: "Forstå hva som skjer i det indre øret og hvordan behandlingen fungerer.",
    href: "#",
    comingSoon: true,
  },
  {
    title: "Jeg trenger en øvelse",
    desc: "Finn riktig manøver eller øvelse for din buegang i øvelsesbiblioteket.",
    href: "/ovelsesbibliotek/",
    comingSoon: false,
  },
  {
    title: "Jeg er fortsatt svimmel",
    desc: "Restsymptomer etter krystallsyke er vanlig — les hva som kan hjelpe.",
    href: "#",
    comingSoon: true,
  },
];
---

<Layout
  title="Krystallsykehjelpen | Behandling av krystallsyke i Fetsund"
  description="Sliter du med svimmelhet eller krystallsyke (BPPV)? Vi tilbyr klinikk-, hjemme- og telefonkonsultasjoner i Fetsund. Bestill time i dag."
  activePath="/"
>
  <!-- 1. Hero -->
  <section class="hero">
    <div class="container hero-grid">
      <div>
        <span class="eyebrow">Ekspertise innen krystallsyke og svimmelhet</span>
        <h1>{sections.hero?.heading ?? (<>Trygg kunnskap når verden <em>plutselig snurrer</em></>)}</h1>
        <p class="lead" set:html={sections.hero?.body?.[0]
          ? inlineHtmlFromBlock(sections.hero.body[0])
          : "Krystallsyke (BPPV) er den vanligste årsaken til plutselig, kortvarig svimmelhet. Her finner du pålitelig kunnskap, øvelser og veien til rask hjelp – i klinikken, hjemme hos deg, eller over telefon."} />
        <div class="btn-row">
          <a href={bookingUrl} class="btn btn-primary">Bestill time →</a>
          <a href="/krystallsyken/" class="btn btn-outline">Les om krystallsyke →</a>
        </div>
        <ul class="trust-row">
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10" /><path d="M9 12l2 2 4-4" /></svg> Faglig forankret</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10" /><path d="M9 12l2 2 4-4" /></svg> Enkelt forklart</li>
          <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10" /><path d="M9 12l2 2 4-4" /></svg> Gratis kunnskap</li>
        </ul>
      </div>
      <div class="hero-media">
        <img src={urlForImage(sections.hero?.mediaAsset, 1200) ?? "/hero-krystallsykehjelpen-finpusset.png"} alt="Kvinne som holder seg på hodet og kjenner på svimmelhet" class="hero-photo" />
      </div>
    </div>
    <svg class="hero-wave" viewBox="0 0 1440 60" preserveAspectRatio="none">
      <path d="M0,32 C360,72 1080,0 1440,32 L1440,60 L0,60 Z" fill="#ffffff" />
    </svg>
  </section>

  <!-- 2. Hvor er du i forløpet? -->
  <section class="section-alt no-top">
    <div class="container">
      <div class="section-head">
        <h2>Hvor er du i forløpet?</h2>
        <hr class="heading-rule" />
      </div>
      <div class="journey-grid">
        {journeyCards.map((card) => (
          <a href={card.href} class={`card journey-card ${card.comingSoon ? "coming-soon" : ""}`} title={card.comingSoon ? "Kommer snart" : undefined}>
            <div class="card-body">
              <h3>{card.title}</h3>
              <p>{card.desc}</p>
              <span class="card-link">{card.comingSoon ? "Kommer snart" : "Les mer →"}</span>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- 3. Forklaringsseksjon om det indre øret -->
  <section>
    <div class="container">
      <div class="diagram-grid">
        <div>
          <span class="eyebrow">Om krystallsyke</span>
          <h2>{sections["hva-skjer-i-oret"]?.heading ?? "Hva skjer i øret?"}</h2>
          <hr class="heading-rule" />
          {sections["hva-skjer-i-oret"]?.body?.length ? (
            sections["hva-skjer-i-oret"].body.map((b) => <p set:html={inlineHtmlFromBlock(b)} />)
          ) : (
            <>
              <p>I det indre øret har vi tre bueganger som registrerer bevegelse. I buegangene finnes det små krystaller (otolitter) som normalt sitter fast i et eget område.</p>
              <p>Når krystallene løsner og havner i en buegang, kan de gi signaler om bevegelse selv når du er i ro. Det er dette som skaper svimmelheten når du endrer stilling.</p>
            </>
          )}
          <a href="/krystallsyken/" class="card-link">Les mer om hvordan balanseorganet fungerer →</a>
        </div>
        <div class="diagram-illustration">
          <img src={urlForImage(sections["hva-skjer-i-oret"]?.mediaAsset, 760) ?? "/ear-diagram.jpg"} alt="Illustrasjon av buegangene i det indre øret" class="diagram-img" />
          <p class="diagram-caption">Illustrasjon av det indre ørets balanseorgan. Ved krystallsyke (BPPV) har otokonier løsnet fra utriculus og kommet inn i en av buegangene, hvor de påvirker væskebevegelsen som igjen resulterer i svimmelhet.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- 4. Øvelser-CTA-bånd -->
  <section class="section-cta">
    <div class="container cta-inner">
      <div>
        <h2>{sections.ovelser?.heading ?? "Øv riktig — trygt hjemmefra"}</h2>
        <p class="sub" set:html={sections.ovelser?.body?.[0]
          ? inlineHtmlFromBlock(sections.ovelser.body[0])
          : "Se øvelsesbiblioteket med manøvre og habitueringsøvelser organisert etter buegang og behov."} />
      </div>
      <a href="/ovelsesbibliotek/" class="btn btn-outline">Til øvelsesbiblioteket →</a>
    </div>
  </section>

  <!-- 5. Siste artikler -->
  <section class="section-alt">
    <div class="container">
      <div class="section-head">
        <h2>Siste artikler</h2>
        <hr class="heading-rule" />
      </div>
      {latestPosts.length ? (
        <div class="card-grid articles-grid">
          {latestPosts.map((post) => (
            <a href={`/blogg/${post.slug}/`} class="card article-card">
              {urlForImage(post.coverImage, 640) ? (
                <img src={urlForImage(post.coverImage, 640)} alt={post.title} class="article-cover" />
              ) : (
                <MediaPlaceholder label={post.title} />
              )}
              <div class="card-body">
                {post.category && <span class="category-badge">{post.category}</span>}
                {post.publishedDate && <span class="post-date">{formatDate(post.publishedDate)}</span>}
                <h3>{post.title}</h3>
                {post.excerpt && <p>{post.excerpt}</p>}
              </div>
            </a>
          ))}
        </div>
      ) : (
        <p>Ingen artikler publisert ennå.</p>
      )}
      <p class="mt"><a href="/blogg/" class="card-link">Se alle artikler →</a></p>
    </div>
  </section>

  <!-- 6. Finn behandler-bånd -->
  <section class="section-beige">
    <div class="container cta-band">
      <div>
        <h2>Trenger du en behandler nær deg?</h2>
        <p>Se kontaktinfo, adresse og kart til Krystallsykehjelpen.</p>
      </div>
      <a href="/finn-behandler/" class="btn btn-primary">Finn behandler →</a>
    </div>
  </section>

  <!-- 7. Faglig ansvar / Marie -->
  <section>
    <div class="container">
      <div class="bio-card">
        <img src={urlForImage(sections["marie-bio"]?.mediaAsset, 240) ?? "/marie-portrait.jpg"} alt="Marie, kiropraktor hos Krystallsykehjelpen" class="bio-photo" />
        <div>
          <h3>{sections["marie-bio"]?.heading ?? "Marie – spesialisert på svimmelhet"}</h3>
          <blockquote class="pull-quote">«Krystallsyke er ufarlig, men skremmende når man ikke vet hva det er. Målet mitt er at du skal forstå hva som skjer — og få hjelp raskt.»</blockquote>
          <p set:html={sections["marie-bio"]?.body?.[0]
            ? inlineHtmlFromBlock(sections["marie-bio"].body[0])
            : "Kiropraktor med spesialkompetanse på utredning og behandling av svimmelhet og krystallsyke."} />
          <a href="/om-krystallsykehjelpen/" class="card-link">Les mer om Krystallsykehjelpen →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- 8. Nyhetsbrev — ingen backend finnes ennå (verken Web3Forms eller
       annet skjemaoppsett i prosjektet). Rent frontend-skjema uten
       innsendingslogikk, flagget i implementasjonsrapporten. -->
  <section class="section-alt">
    <div class="container newsletter">
      <h2>Hold deg oppdatert</h2>
      <p>Få nye artikler om krystallsyke og svimmelhet rett i innboksen.</p>
      <form class="newsletter-form" onsubmit="event.preventDefault();">
        <input type="email" name="email" placeholder="Din e-postadresse" required />
        <button type="submit" class="btn btn-primary">Meld meg på</button>
      </form>
      <p class="newsletter-note">Ingen innsendingslogikk er koblet til ennå — se implementasjonsrapporten.</p>
    </div>
  </section>
</Layout>

<style>
  .hero { padding: 56px 0 90px; position: relative; overflow: hidden; }
  .hero-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 48px; align-items: center; }
  .eyebrow { font-size: 0.82rem; letter-spacing: 0.08em; text-transform: uppercase; color: var(--accent); font-weight: 700; margin-bottom: 10px; display: inline-block; }
  .lead { font-size: 1.1rem; max-width: 46ch; }
  .hero-wave { position: absolute; bottom: -2px; left: 0; width: 100%; line-height: 0; }
  .btn-row { display: flex; gap: 14px; flex-wrap: wrap; margin-top: 26px; }
  @media (max-width: 800px) { .hero-grid { grid-template-columns: 1fr; } }

  .hero-media { position: relative; }
  .hero-photo { width: 100%; border-radius: var(--radius); aspect-ratio: 4/3; object-fit: cover; box-shadow: 0 20px 50px rgba(22,56,117,0.12); }

  .trust-row { list-style: none; display: flex; gap: 20px; flex-wrap: wrap; margin: 24px 0 0; padding: 0; }
  .trust-row li { display: flex; align-items: center; gap: 6px; font-size: 0.85rem; font-weight: 600; color: var(--navy); }
  .trust-row svg { width: 18px; height: 18px; color: #2fbf7a; flex-shrink: 0; }

  .section-alt { background: var(--paper); padding: 60px 0; }
  .section-alt.no-top { padding-top: 0; padding-bottom: 70px; }
  .section-beige { background: var(--beige); padding: 60px 0; }
  section { padding: 60px 0; }

  .section-head { max-width: 62ch; margin-bottom: 36px; }
  .section-alt.no-top .section-head { text-align: center; margin-left: auto; margin-right: auto; }
  .section-alt.no-top .section-head .heading-rule { margin-left: auto; margin-right: auto; }

  .journey-grid { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 20px; }
  @media (max-width: 980px) { .journey-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); } }
  @media (max-width: 560px) { .journey-grid { grid-template-columns: 1fr; } }
  .journey-card {
    background: var(--paper); border: 1px solid var(--line); border-radius: var(--radius);
    text-decoration: none; display: block; transition: transform 0.15s ease, box-shadow 0.15s ease;
    box-shadow: 0 12px 28px rgba(22, 56, 117, 0.06);
  }
  .journey-card:hover { transform: translateY(-2px); box-shadow: 0 18px 36px rgba(22, 56, 117, 0.1); }
  .journey-card .card-body { padding: 24px; }
  .journey-card h3 { font-size: 1rem; margin: 0 0 8px; }
  .journey-card p { font-size: 0.88rem; margin: 0 0 12px; color: var(--ink-soft); }
  .journey-card.coming-soon { opacity: 0.7; cursor: default; }
  .journey-card.coming-soon .card-link { color: var(--ink-soft); }

  .diagram-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 48px; align-items: center; }
  @media (max-width: 800px) { .diagram-grid { grid-template-columns: 1fr; } }
  .diagram-illustration { background: var(--sky); border-radius: var(--radius); padding: 30px; }
  .diagram-img { width: 100%; max-width: 380px; margin: 0 auto; display: block; }
  .diagram-caption { font-size: 0.82rem; color: var(--ink-soft); text-align: center; margin: 14px auto 0; max-width: 380px; line-height: 1.5; }

  .icon-circle {
    width: 52px; height: 52px; border-radius: 50%; background: var(--sky);
    display: flex; align-items: center; justify-content: center; margin-bottom: 14px;
  }
  .icon-circle svg { width: 26px; height: 26px; color: var(--navy-2); }
  .card-link { display: inline-flex; align-items: center; gap: 6px; font-weight: 700; font-size: 0.9rem; text-decoration: none; color: var(--navy-2); margin-top: 4px; }
  .mt { margin-top: 20px; }

  .section-cta { background: var(--navy); color: #fff; padding: 48px 0; }
  .section-cta .cta-inner { display: flex; gap: 24px; align-items: center; justify-content: space-between; flex-wrap: wrap; }
  .section-cta h2 { color: #fff; margin-bottom: 8px; }
  .section-cta .sub { color: #d9e9f6; margin: 0; max-width: 52ch; }
  .btn-outline { background: var(--beige); color: var(--navy); }
  @media (max-width: 700px) { .section-cta .cta-inner { flex-direction: column; align-items: flex-start; } }

  .card-grid { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 22px; }
  @media (max-width: 860px) { .card-grid { grid-template-columns: 1fr; } }
  .card {
    background: var(--paper); border: 1px solid var(--line); border-radius: var(--radius);
    overflow: hidden; box-shadow: 0 18px 36px rgba(22, 56, 117, 0.08);
    text-decoration: none; display: block; transition: transform 0.15s ease, box-shadow 0.15s ease;
  }
  .card:hover { transform: translateY(-2px); box-shadow: 0 22px 44px rgba(22, 56, 117, 0.12); }
  .article-cover { width: 100%; aspect-ratio: 16/10; object-fit: cover; }
  .card-body { padding: 22px; }
  .card-body h3 { margin: 4px 0 8px; }
  .card-body p { margin: 0; color: var(--ink-soft); }
  .post-date { font-size: 0.8rem; font-weight: 700; color: var(--accent); text-transform: uppercase; letter-spacing: 0.04em; }
  .category-badge {
    display: inline-block; font-size: 0.72rem; font-weight: 700; padding: 3px 10px;
    border-radius: 999px; background: var(--sky); color: var(--navy-2); margin-bottom: 6px; margin-right: 6px;
  }

  .cta-band { border-radius: var(--radius); padding: 36px; display: flex; justify-content: space-between; align-items: center; gap: 24px; flex-wrap: wrap; }
  .cta-band h2 { margin-bottom: 6px; }
  .cta-band p { margin: 0; }
  @media (max-width: 700px) { .cta-band { flex-direction: column; align-items: flex-start; } }

  .bio-card { background: var(--sky); border-radius: var(--radius); padding: 26px; display: flex; gap: 22px; align-items: center; }
  .bio-photo { width: 84px; height: 84px; border-radius: 50%; object-fit: cover; flex-shrink: 0; }
  .bio-card h3 { margin-bottom: 4px; }
  .bio-card p { margin-bottom: 6px; }
  .pull-quote {
    margin: 0 0 14px; padding-left: 16px; border-left: 3px solid var(--turquoise);
    font-family: var(--font-display); font-style: italic; color: var(--navy); font-size: 1.02rem;
  }
  @media (max-width: 600px) { .bio-card { flex-direction: column; text-align: center; } .pull-quote { border-left: none; padding-left: 0; } }

  .newsletter { max-width: 560px; text-align: center; }
  .newsletter h2 { margin-bottom: 8px; }
  .newsletter-form { display: flex; gap: 10px; flex-wrap: wrap; margin-top: 20px; justify-content: center; }
  .newsletter-form input {
    flex: 1 1 240px; padding: 12px 16px; border-radius: 999px; border: 1px solid var(--line);
    font-family: var(--font-body); font-size: 0.94rem;
  }
  .newsletter-note { font-size: 0.76rem; color: var(--ink-soft); margin-top: 10px; }
</style>
